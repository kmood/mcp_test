export declare class TimeutilService {
    formatDate(date: number): string;
    getNow(now?: Date): string;
    getWeekday(date: Date): number;
    getMonth(date: Date): number;
    getDay(date: Date): number;
    getHhMm(date: Date): string;
}
